# Trang Đăng Nhập (Login Page)

Chào mừng đến với dự án **Trang Đăng Nhập**! Đây là một mẫu giao diện đăng nhập đơn giản nhưng hiện đại, được xây dựng bằng HTML, CSS và JavaScript thuần.

## 🌟 Giới thiệu

Dự án này cung cấp một giao diện người dùng (UI) sạch sẽ và thân thiện cho chức năng đăng nhập. Nó bao gồm các tính năng cơ bản cần thiết như validation form (kiểm tra dữ liệu nhập) và phản hồi người dùng, phù hợp để tích hợp vào các website hoặc ứng dụng web lớn hơn.

## ✨ Tính năng chính

- **Giao diện Hiện đại**: Thiết kế đẹp mắt, sử dụng CSS để tạo kiểu cho các ô input, nút bấm và bố cục chung.
- **Responsive**: Hiển thị tốt trên các kích thước màn hình khác nhau (mặc dù tập trung chính vào giao diện desktop/tablet).
- **Kiểm tra dữ liệu (Validation)**:
  - Tự động kiểm tra tên đăng nhập (yêu cầu nhập, độ dài tối thiểu).
  - Tự động kiểm tra mật khẩu (yêu cầu nhập, độ dài tối thiểu).
  - Hiển thị thông báo lỗi ngay lập tức khi người dùng nhập sai.
- **Tương tác người dùng**:
  - Thông báo thành công hoặc thất bại khi nhấn nút "Đăng Nhập".
  - Hiệu ứng focus khi chọn các ô input.
- **Tên đăng nhập & Mật khẩu mẫu**:
  - Dự án có sẵn tài khoản demo trong code để test thử tính năng đăng nhập thành công.

## 🛠️ Công nghệ sử dụng

- **HTML5**: Xây dựng cấu trúc trang web.
- **CSS3**: Tạo kiểu, màu sắc và bố cục cho giao diện.
- **JavaScript (Vanilla)**: Xử lý logic, bắt sự kiện và kiểm tra dữ liệu form.

## 🚀 Hướng dẫn Cài đặt & Sử dụng

Bạn không cần cài đặt bất kỳ môi trường phức tạp nào để chạy dự án này.

1.  **Tải về**: Clone hoặc tải file nén của dự án về máy tính.
2.  **Mở dự án**:
    - Truy cập vào thư mục chứa dự án `loginpage`.
    - Nhấn đúp chuột vào file `index.html` để mở nó trên trình duyệt web của bạn (Chrome, Firefox, Edge, Safari...).
3.  **Thử nghiệm**:
    - Nhập tên đăng nhập bất kỳ để xem validation.
    - Sử dụng tài khoản demo sau để thử đăng nhập thành công:
      - **Username**: `admin`
      - **Password**: `123456`
    - Nhấn **F12** (hoặc mở Console của trình duyệt) để xem thêm các log nếu cần.

## 📂 Cấu trúc thư mục

- `index.html`: File chính chứa cấu trúc HTML của trang đăng nhập.
- `style.css`: File chứa toàn bộ mã CSS để trang trí giao diện.
- `script.js`: File chứa mã nguồn JavaScript để xử lý logic validate và đăng nhập.
- `README.md`: File hướng dẫn này.

## 🤝 Đóng góp

Mọi đóng góp để cải thiện dự án đều được hoan nghênh. Hãy thoải mái tạo pull request hoặc mở issue nếu bạn tìm thấy lỗi.

---

_Chúc bạn có trải nghiệm tốt với dự án này!_
